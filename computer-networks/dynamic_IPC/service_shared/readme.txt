chmod +x run.sh
run run.sh first to complete the setup ./run.sh
then service_coord starts running service objects are up and running
fifo names from clients are written in fmfifo
enter service names to coordinate
run clients with arg as their name to write it to server and also crate fifo of same name with r and rw appended
